package nous.data

/**

class Batch[A](val samples: List[Sample[A]]) {
  val length = samples.length
  val depth = samples.head.depth
  val rows = samples.head.rows
  val cols = samples.head.cols
  val size = length * depth * rows * cols
}

 */